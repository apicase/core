{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome debugger",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceRoot}"
    }
  ]
}